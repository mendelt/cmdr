[package]
name = "cmdr_macro"
version = "0.1.0"
authors = ["Mendelt Siebenga <mendelt@siebenga.com>"]
edition = "2018"

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "0.4"
syn = { version = "0.15", features = ["extra-traits", "full", "parsing"] }
quote = "0.6"
